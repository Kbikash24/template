export default function PromoBar() {
  return (
    <div
      style={{
        background: "#f8fbff",
        borderBottom: "1px solid var(--line)",
        color: "var(--primary)",
        textAlign: "center",
        padding: "10px 16px",
        fontSize: "0.92rem",
        fontWeight: 700,
      }}
    >
      New batches are now live for UPSC, GATE, SSC, and Banking. Start with a
      free class today.
    </div>
  );
}
